# Routes vitrine — mabb.fr (et localhost en dev)
vitrine_controllers:
    resource:
        path: ../../src/Controller/Vitrine/
        namespace: App\Controller\Vitrine
    type: attribute
    host: '{domain}'
    defaults:
        domain: localhost
    requirements:
        domain: 'mabb\.fr|localhost'
