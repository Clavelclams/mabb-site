{% extends 'vitrine/base.html.twig' %}
{% block title %}Venaball — Bientôt disponible{% endblock %}
{% block meta_description %}Venaball, l'espace joueur de la MABB (l'espace joueuse du club) — en cours de développement.{% endblock %}

{% block body %}
<div class="page-header">
    <div class="container text-center">
        <h1><i class="bi bi-person-badge-fill me-2"></i>Venaball</h1>
        <p style="color:rgba(255,255,255,.75)">l'espace joueuse du club — l'espace de chaque joueuse</p>
        <nav aria-label="breadcrumb" class="mt-3">
            <ol class="breadcrumb justify-content-center">
                <li class="breadcrumb-item"><a href="{{ path('vitrine_accueil') }}" style="color:#fff">Accueil</a></li>
                <li class="breadcrumb-item"><a href="{{ path('vitrine_numerique') }}" style="color:#fff">Numérique</a></li>
                <li class="breadcrumb-item active" style="color:#fc702a;font-weight:600">Venaball</li>
            </ol>
        </nav>
    </div>
</div>

<section class="section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8 text-center">
                <div class="card card-mabb p-5">
                    <div class="mx-auto mb-4" style="width:120px;height:120px;border-radius:50%;background:rgba(252,112,42,.15);display:flex;align-items:center;justify-content:center">
                        <i class="bi bi-tools" style="font-size:3rem;color:var(--mabb-orange)"></i>
                    </div>
                    <span class="badge mb-3 mx-auto" style="background:var(--mabb-orange);color:#fff;font-size:.85rem;padding:6px 16px">🚧 Site en construction</span>
                    <h2 class="fw-bold mb-3" style="color:var(--mabb-dark)">Venaball arrive bientôt</h2>
                    <p class="text-muted">
                        Ton espace perso de joueuse — profil, photo, bilan, shot chart, stats, progression —
                        est en cours de développement. Il sera accessible sur
                        <strong>pirb.mabb.fr</strong> à son ouverture.
                    </p>
                    <p class="text-muted mb-4">
                        Chaque joueuse aura sa page perso style réseau social, avec ce qu'elle choisit de partager ou pas.
                    </p>
                    <div class="d-flex flex-column flex-sm-row justify-content-center gap-3">
                        <a href="{{ path('vitrine_numerique') }}" class="btn btn-mabb rounded-pill px-4">
                            <i class="bi bi-arrow-left me-1"></i>Retour à l'espace Numérique
                        </a>
                        <a href="{{ path('vitrine_accueil') }}" class="btn btn-outline-secondary rounded-pill px-4">
                            <i class="bi bi-house me-1"></i>Accueil
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}
