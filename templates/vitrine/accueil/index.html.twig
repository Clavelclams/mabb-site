{% extends 'vitrine/base.html.twig' %}

{% block title %}MABB — Amiens Métropole Basket Ball | Club de basket féminin
{% endblock %}
{% block meta_description %}Club de basket-ball exclusivement féminin à Amiens. Triple label FFBB 3 étoiles. Formation, compétition, 3x3 et inclusion par le sport.
{% endblock %}

{% block body %}

	{# ===== HERO ===== #}
	<section class="hero-accueil">
		<div class="hero-accueil-bg"></div>
		<div class="container position-relative">
			<div class="row align-items-center g-5">
				<div class="col-lg-6">
					{# [CMS V2] Blocs éditables depuis /admin/contenus — le texte
					   ci-dessous est le DÉFAUT, remplacé si l'admin a saisi mieux #}
					<span class="badge-label mb-3">
						<i class="bi bi-star-fill"></i>
						{{ cms('accueil.hero.badge', 'Triple label FFBB 3 étoiles') }}</span>
					<h1 class="display-4 fw-bold mb-3 hero-title">
						{{ cms('accueil.hero.titre_ligne1', 'Amiens Métropole') }}<br>
						<span class="text-mabb-orange">{{ cms('accueil.hero.titre_ligne2', 'Basket Ball') }}</span>
					</h1>
					<p class="lead mb-4 hero-desc">
						{{ cms('accueil.hero.description', "Club de basket-ball exclusivement féminin implanté sur le territoire d'Amiens Métropole. De l'initiation à la performance, nous formons les championnes de demain.", 'long') }}
					</p>
					<div class="d-flex flex-wrap gap-3">
						<a href="{{ path('vitrine_compte_se_connecter') }}" class="btn btn-mabb btn-lg">
							<i class="bi bi-person-plus me-2"></i>Rejoindre le club
						</a>
						<a href="{{ path('vitrine_club') }}" class="btn btn-outline-light btn-lg rounded-pill">
							<i class="bi bi-info-circle me-2"></i>Découvrir
						</a>
						<a href="https://le-savoir-pour-tous.pass-education.fr/inscription?slug=mabb-amiens-metropole-basket-ball&token=b8db9981-d307-4e79-a539-ac30aafd2041" target="_blank" class="btn btn-lg rounded-pill btn-aide-devoir">
							<i class="bi bi-book-half me-2"></i>Aide au devoir
						</a>
						<a href="{{ path('vitrine_nos_reseaux') }}"
						   class="btn btn-lg rounded-pill fw-semibold"
						   style="background:rgba(255,255,255,.15);color:#fff;border:1px solid rgba(255,255,255,.4);backdrop-filter:blur(4px)">
							<i class="bi bi-share me-2"></i>Nos réseaux
						</a>
					</div>
				</div>
				<div class="col-lg-6 text-center">
					<div class="p-3 bg-white rounded-4 shadow-lg d-inline-block hero-photo-wrap">
						{# [CMS V2] Photo du hero remplaçable depuis /admin/contenus #}
						<img src="{{ asset(cms_img('accueil.hero.photo', 'images/manar.jpg')) }}" alt="Joueuse MABB" class="img-fluid rounded-3 hero-photo">
					</div>
				</div>
			</div>
		</div>
	</section>

	{# ===== CHIFFRES CLÉS ===== #}
	<section class="section-chiffres" id="section-chiffres">
		<div class="container">
			<div class="row g-3 justify-content-center">

                {# Compteurs éditables via le CMS (bloc cms('clé','défaut')) : la
                   valeur reste NUMÉRIQUE pour que l'animation de comptage marche ;
                   le suffixe (« + ») est géré à part par data-suffix. L'admin les
                   ajuste dans /admin/contenus, plus aucun chiffre en dur ici. #}
                {# Licenciées — animé de 0 à la valeur CMS #}
                <div class="col-6 col-md-3">
                    <div class="card card-mabb text-center p-4">
                        <i class="bi bi-people-fill text-mabb-orange chiffre-icon"></i>
                        <div class="display-6 fw-bold mt-2 chiffre-number counter-anim"
                             data-target="{{ cms('accueil_nb_licenciees', '373') }}"
                             data-suffix="">0</div>
                        {# [V2.4g] Défaut dynamique. ⚠️ La clé CMS est déjà enregistrée en prod
                           avec « Saison 2025/26 » : mettre à jour le libellé dans Admin → Contenus. #}
                        <div class="text-muted small">{{ cms('accueil_label_licenciees', 'Licenciées — Saison ' ~ saison_active()) }}</div>
                    </div>
                </div>

                <div class="col-6 col-md-3">
                    <div class="card card-mabb text-center p-4">
                        <i class="bi bi-trophy-fill text-mabb-orange chiffre-icon"></i>
                        <div class="display-6 fw-bold mt-2 chiffre-number counter-anim" data-target="{{ cms('accueil_nb_labels', '3') }}" data-suffix="">0</div>
                        <div class="text-muted small">Labels FFBB 3★</div>
                    </div>
                </div>
                <div class="col-6 col-md-3">
                    <div class="card card-mabb text-center p-4">
                        <i class="bi bi-building text-mabb-orange chiffre-icon"></i>
                        <div class="display-6 fw-bold mt-2 chiffre-number counter-anim" data-target="{{ cms('accueil_nb_quartiers', '7') }}" data-suffix="">0</div>
                        <div class="text-muted small">Quartiers</div>
                    </div>
                </div>
                <div class="col-6 col-md-3">
                    <div class="card card-mabb text-center p-4">
                        <i class="bi bi-calendar-heart text-mabb-orange chiffre-icon"></i>
                        <div class="display-6 fw-bold mt-2 chiffre-number counter-anim" data-target="{{ cms('accueil_nb_annees', '23') }}" data-suffix="+">0</div>
                        <div class="text-muted small">Ans d'engagement</div>
                    </div>
                </div>

            </div>
        </div>
    </div>
</section>

<script>
(function () {
    function easeOutCubic(t) { return 1 - Math.pow(1 - t, 3); }
    function animateCounter(el) {
        var target = parseInt(el.dataset.target, 10);
        var suffix = el.dataset.suffix || '';
        var duration = 2200;
        var start = performance.now();
        function tick(now) {
            var elapsed = now - start;
            var progress = Math.min(elapsed / duration, 1);
            var current = Math.round(target * easeOutCubic(progress));
            el.textContent = current.toLocaleString('fr-FR') + suffix;
            if (progress < 1) requestAnimationFrame(tick);
        }
        requestAnimationFrame(tick);
    }
    var counters = document.querySelectorAll('.counter-anim');
    var observer = new IntersectionObserver(function (entries) {
        entries.forEach(function (entry) {
            if (entry.isIntersecting && !entry.target.dataset.done) {
                entry.target.dataset.done = '1';
                animateCounter(entry.target);
            }
        });
    }, { threshold: 0.25 });
    counters.forEach(function (el) { observer.observe(el); });
})();
</script>

{# ===== QUI SOMMES-NOUS ===== #}
<section class="section">
    <div class="container">
        <div class="row align-items-center g-5">
            <div class="col-lg-6">
                <h2 class="section-title text-white">{{ cms('accueil.engage.titre_avant', 'Un club engagé pour') }} <span>{{ cms('accueil.engage.titre_accent', 'le basket féminin') }}</span></h2>
                <p class="text-white-force">
                    {{ cms('accueil.engage.paragraphe1', "La MABB porte un projet sportif, éducatif et citoyen structuré. Nous accueillons les jeunes filles de 3 à 25+ ans dans un cadre bienveillant et exigeant. Notre mission dépasse le terrain : nous faisons du basketball un levier d'émancipation et de rayonnement du sport féminin.", 'long') }}
                </p>
                <p class="text-white-force">
                    {{ cms('accueil.engage.paragraphe2', "Le club mène toute l'année des actions d'intérêt général dans les établissements scolaires et les quartiers prioritaires d'Amiens : école de mini-basket, aide aux devoirs, stages gratuits Génération Basket, tournois 3x3.", 'long') }}
                </p>
                {# ===== Lien Google Avis — URL "Donner un avis" directement =====
                   Pour pointer DIRECTEMENT sur le formulaire d'avis Google, utiliser :
                     https://search.google.com/local/writereview?placeid=<PLACE_ID>

                   📍 Trouver le Place ID MABB :
                     https://developers.google.com/maps/documentation/places/web-service/place-id
                     → chercher "Amiens Métropole Basket Ball" → copier le Place ID (commence par "ChI...")
                     → coller dans la variable `google_review_url` ci-dessous

                   En attendant : URL fallback de la page "Avis" Google de MABB (meilleure qu'une simple
                   recherche, mais ouvre la fiche Lecture seule au lieu du formulaire d'avis direct). #}
                {% set google_review_url = 'https://www.google.com/search?sa=X&sca_esv=13664839709b14a6&sxsrf=ANbL-n60J-gmss4Mh9M31CuHTC_Tp2HPCw:1780390294217&q=Amiens+M%C3%A9tropole+Basket+Ball+(MABB)+Avis&rflfq=1&num=20&stick=H4sIAAAAAAAAAONgkxI2tzQwNDAwNzMwMbU0MjIwtDAx28DI-IpR0zE3MzWvWMH38MqSovyC_JxUBafE4uzUEiCVk6Og4evo5KSp4FiWWbyIlXi1AJdxs7tyAAAA&rldimm=7901007604592201846&tbm=lcl#lkt=LocalPoiReviews' %}
                {# Quand tu as le Place ID, remplace par :
                   {% set google_review_url = 'https://search.google.com/local/writereview?placeid=CHI_PLACE_ID_ICI' %} #}

                <div class="d-flex flex-wrap gap-2 mt-3">
                    <a href="{{ google_review_url }}" target="_blank" rel="noopener" class="badge px-3 py-2 rounded-pill fw-bold badge-label-outline text-decoration-none" title="Donner un avis Google sur MABB"><i class="bi bi-check-circle me-1"></i> Club Formateur <span class="badge-star">★★★</span></a>
                    <a href="{{ google_review_url }}" target="_blank" rel="noopener" class="badge px-3 py-2 rounded-pill fw-bold badge-label-outline text-decoration-none" title="Donner un avis Google sur MABB"><i class="bi bi-check-circle me-1"></i> Citoyenneté <span class="badge-star">★★★</span></a>
                    <a href="{{ google_review_url }}" target="_blank" rel="noopener" class="badge px-3 py-2 rounded-pill fw-bold badge-label-outline text-decoration-none" title="Donner un avis Google sur MABB"><i class="bi bi-check-circle me-1"></i> Mini Basket <span class="badge-star">★★★</span></a>
                </div>
            </div>
            <div class="col-lg-6 text-center">
                <img src="{{ asset(cms_img('accueil.engage.photo', 'images/panierGonflable.jpeg')) }}"
                     alt="Panier de basket MABB"
                     class="img-fluid"
                     style="border:4px solid #fff;border-radius:16px;box-shadow:0 8px 24px rgba(0,0,0,.3);max-height:420px;object-fit:cover;width:100%">
            </div>
        </div>
    </div>
</section>

{# ===== NOS SECTIONS ===== #}
<section class="section section-gray">
    <div class="container">
        <div class="text-center mb-5">
            <h2 class="section-title">Du <span>Micro Basket</span> à la <span>Performance</span></h2>
            <p class="section-subtitle">Un parcours complet de 3 à 25+ ans</p>
        </div>
        <div class="row g-4">
            {% set sections = [
                {icon: 'bi-balloon', title: 'Micro Basket', ages: '3-5 ans', desc: 'Éveil psychomoteur et découverte ludique du basket pour les tout-petits. Matériel adapté en taille et en couleur.', color: '#e74c3c'},
                {icon: 'bi-stars', title: 'Mini Basket', ages: '6-10 ans', desc: 'Apprentissage en s\'amusant avec ballons adaptés (taille 3 et 5) et paniers ajustables. 3 sites : Étouvie, Nord, Est.', color: '#f39c12'},
                {icon: 'bi-lightning', title: 'Compétition', ages: 'U11 à Senior', desc: 'Équipes engagées dans tous les championnats : départemental, régional, pré-national. Formation d\'excellence.', color: 'var(--mabb-blue)'},
                {icon: 'bi-dribbble', title: 'Basket 3x3', ages: '11-25+ ans', desc: 'Ligue MABB officielle FFBB, tournois homologués dans les quartiers, format libre et créatif.', color: '#2ecc71'},
                {icon: 'bi-mortarboard', title: 'Section Sportive', ages: 'Collège', desc: 'Academy César Franck 100% féminine. Horaires aménagés, 4 entraînements/semaine, double projet scolaire-sportif.', color: '#9b59b6'},
                {icon: 'bi-heart', title: 'Actions Citoyennes', ages: 'Toute l\'année', desc: 'Centres Génération Basket gratuits, aide aux devoirs, interventions scolaires, tournois QPV.', color: '#1abc9c'},
            ] %}
				{% for s in sections %}
					<div class="col-md-6 col-lg-4">
						<div class="card card-mabb h-100">
							<div class="card-body p-4">
								<div class="d-flex align-items-center gap-3 mb-3">
									<div style="width:50px;height:50px;border-radius:12px;background:{{ s.color }}15;display:flex;align-items:center;justify-content:center">
										<i class="bi {{ s.icon }}" style="font-size:1.5rem;color:{{ s.color }}"></i>
									</div>
									<div>
										<h5 class="mb-0 fw-bold">{{ s.title }}</h5>
										<small class="text-muted">{{ s.ages }}</small>
									</div>
								</div>
								<p class="text-muted mb-0">{{ s.desc }}</p>
							</div>
						</div>
					</div>
				{% endfor %}
			</div>
		</div>
	</section>

	{# ===== DERNIÈRES ACTUS (placeholder — sera dynamique avec le CMS) ===== #}
	<section class="section">
		<div class="container">
			<div class="d-flex justify-content-between align-items-center mb-4">
				<h2 class="section-title mb-0" style="color:#fff">Dernières
					<span>actualités</span>
				</h2>
				<a href="{{ path('vitrine_news') }}"
				   class="btn rounded-pill fw-semibold"
				   style="background:#fc702a;color:#fff;border:none">Voir tout
					<i class="bi bi-arrow-right ms-1"></i>
				</a>
			</div>
			<div class="row g-4">
				{% if dernieres_actus|length > 0 %}
					{% for actu in dernieres_actus %}
						<div class="col-md-4">
							<div class="card card-mabb h-100">
								{% if actu.imagePath %}
									<img src="{{ asset('uploads/articles/' ~ actu.imagePath) }}" alt="{{ actu.titre }}" style="height:180px;object-fit:cover;width:100%">
								{% endif %}
								<div class="card-body p-4">
									<small class="text-muted">
										<i class="bi bi-calendar3 me-1"></i>
										{{ actu.publishedAt ? actu.publishedAt|date('d/m/Y') : '' }}
									</small>
									<h5 class="fw-bold mt-2 mb-2">{{ actu.titre }}</h5>
									<p class="text-muted">{{ actu.contenu|striptags|slice(0, 120) }}...</p>
									<a href="{{ path('vitrine_news_article', {slug: actu.slug}) }}" class="btn btn-sm rounded-pill mt-2 fw-semibold" style="background:var(--mabb-blue);color:#fff;border:none">
										Lire la suite
										<i class="bi bi-arrow-right"></i>
									</a>
								</div>
							</div>
						</div>
					{% endfor %}
				{% else %}
					<div class="col-12 text-center py-4">
						<p class="text-white fw-semibold">Aucune actualité pour le moment.</p>
					</div>
				{% endif %}
			</div>
		</div>
	</section>

		{# ===== DERNIÈRES PHOTOS — BLOC 4 V2 =====
		   Affiche les 6 photos les plus récentes en mosaïque cliquable.
		   Lien vers la galerie complète en bas. #}
		<section class="section section-gray">
			<div class="container">
				<div class="text-center mb-5">
					<h2 class="section-title">Dernières
						<span>photos</span>
					</h2>
					<p class="section-subtitle">Le club en images — entraînements, matchs, animations</p>
				</div>
				{% if dernieres_photos is defined and dernieres_photos|length > 0 %}
					<div class="row g-3">
						{% for photo in dernieres_photos %}
							<div class="col-6 col-md-4 col-lg-2">
								<a href="{{ path('vitrine_galerie') }}" class="d-block text-decoration-none">
									<div style="aspect-ratio:1/1; overflow:hidden; border-radius:12px; position:relative; box-shadow:0 2px 8px rgba(0,0,0,.1); transition:transform .25s, box-shadow .25s;"
									     onmouseover="this.style.transform='scale(1.03)';this.style.boxShadow='0 8px 24px rgba(0,0,0,.18)'"
									     onmouseout="this.style.transform='scale(1)';this.style.boxShadow='0 2px 8px rgba(0,0,0,.1)'">
										<img src="{{ asset('uploads/galerie/' ~ photo.path) }}"
										     alt="{{ photo.legende ?: photo.nom }}"
										     loading="lazy"
										     style="width:100%; height:100%; object-fit:cover; display:block;">
										{% if photo.legende %}
											<div style="position:absolute; bottom:0; left:0; right:0; padding:8px 10px; background:linear-gradient(to top, rgba(0,0,0,.7), transparent); color:#fff; font-size:.75rem;">
												{{ photo.legende }}
											</div>
										{% endif %}
									</div>
								</a>
							</div>
						{% endfor %}
					</div>
					<div class="text-center mt-4">
						<a href="{{ path('vitrine_galerie') }}" class="btn rounded-pill px-4 py-2 fw-semibold"
						   style="background:var(--mabb-orange); color:#fff; border:2px solid var(--mabb-orange); transition:all .2s;"
						   onmouseover="this.style.background='#fff'; this.style.color='var(--mabb-orange)';"
						   onmouseout="this.style.background='var(--mabb-orange)'; this.style.color='#fff';">
							<i class="bi bi-images me-1"></i>Voir toute la galerie
							<i class="bi bi-arrow-right ms-1"></i>
						</a>
					</div>
				{% else %}
					<div class="text-center py-4">
						<p class="text-muted">Aucune photo pour le moment.</p>
					</div>
				{% endif %}
			</div>
		</section>

	{# ===== CTA INSCRIPTION : ===== #}
	<section class="section-cta-inscription">
		<div class="container">
			<h2 class="display-5 fw-bold mb-3">Rejoins la
				<span class="text-mabb-orange">MABB</span>
			</h2>
			<p class="lead mb-4 cta-desc">
				De 3 à 25+ ans, il y a une place pour toi. Inscriptions ouvertes toute l'année.
			</p>
			<div class="d-flex flex-wrap gap-3 justify-content-center">
				<a href="{{ path('vitrine_compte_s_inscrire') }}" class="btn btn-mabb btn-lg">
					<i class="bi bi-person-plus me-2"></i>S'inscrire</a>
				<a href="mailto:contact@mabb.fr" class="btn btn-outline-light btn-lg rounded-pill">
					<i class="bi bi-envelope me-2"></i>contact@mabb.fr</a>
			</div>
		</div>
	</section>

{% endblock %}
